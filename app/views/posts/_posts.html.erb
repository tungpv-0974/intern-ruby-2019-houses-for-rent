<div class="container mt-5 mb-5">
  <section class="dark-grey-text">
    <h2 class="text-center font-weight-bold mb-4 pb-2"><%= t ".recent_post" %></h2>
    <div class ="paginator"><%= paginate @posts, remote: true %></div>
    <ul class="users", id="posts">
      <%= render @posts %>
    </ul>
    <div class ="paginator"><%= paginate @posts, remote: true %></div>
  </section>
</div>
